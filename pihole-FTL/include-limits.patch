diff --git a/src/FTL.h b/src/FTL.h
index 38c1485..c716781 100644
--- a/src/FTL.h
+++ b/src/FTL.h
@@ -44,6 +44,7 @@
 // Interfaces
 #include <ifaddrs.h>
 #include <net/if.h>
+#include <limits.h>
 
 // Define MIN and MAX macros, use them only when x and y are of the same type
 #define MAX(x,y) (((x) > (y)) ? (x) : (y))
